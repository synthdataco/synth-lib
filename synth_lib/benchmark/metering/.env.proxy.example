# Copy to .env.proxy (NEVER commit .env.proxy — it is gitignored, this example file is NOT).
# Never put a real key here.
LITELLM_MASTER_KEY=sk-local-master-CHANGE-ME
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
# engy.ai (glm-5.2), served through the claude-code adapter — see litellm_config.yaml
ENGY_API_KEY=
# Vercel AI Gateway (qwen3.8-max) — see litellm_config.yaml
AI_GATEWAY_API_KEY=
